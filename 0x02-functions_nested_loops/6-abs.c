#include "main.h"

/**
 * _abs - function to compute the absolute value of an integer
 * @n: the int to be checked
 * Return: the absolute value of n
 *
 * Description: This program used boolena operators to compute
 * the absolute value of integer n.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
