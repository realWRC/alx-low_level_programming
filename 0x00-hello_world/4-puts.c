#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 *
 * Description: This program takes the string in Array A and sends
 * it to standard output with the puts function.
 */

int main(void)
{
	char A[] = "\"Programming is like building a multilingual puzzle";

	puts(A);
	return (0);
}
