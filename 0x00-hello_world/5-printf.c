#include <stdio.h>

/**
 *main - Entry point
 *Return: Always 0 (Success)
 *
 *Description: This program prints "with proper grammar,
 *but the outcome is a piece of art," using the printf
 *function
 */

int main(void)
{
	char A[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", A);
	return (0);
}
